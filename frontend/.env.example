# Copia este archivo a .env.local (no se versiona) y completa con los valores de tu Entra ID.
VITE_TENANT_ID=
VITE_FRONTEND_CLIENT_ID=
VITE_API_CLIENT_ID=
VITE_REDIRECT_URI=http://localhost:3000
# Vacío en desarrollo (usa el proxy de Vite al BFF). En producción: URL de AWS API Gateway.
VITE_API_BASE_URL=
